# Ozon 多店铺数据智能平台 — 项目讲解稿

> 用途：面试/汇报前过一遍，照着讲即可。少技术、多业务。

---

## 一、一句话介绍

我搭建了一套 Ozon 多店铺的数据智能平台，把**店铺后台、公司数据中台、运营的表格**三处数据打通，统一成 SKU 级的利润、销量、库存 BI 看板；并做了成本利润、销量预测补货、物流直发三个计算模块，接入钉钉机器人自动通知货件状态，服务运营和供应链两个部门。

---

## 二、为什么要做（讲这段是为了让后面的方案显得必要）

我们是 Ozon 多店铺卖家，之前的状况是：

- 数据是散的：店铺后台一套、公司数据中台一套、运营自己在表格里维护一套。
- **同一个 SKU，利润、库存、销量对不上**，运营和供应链各看各的，对账要扯皮。
- 决策靠拍脑袋：哪个 SKU 到底赚不赚钱、该不该补货、货到哪了，没有统一答案。

所以我要解决的核心问题就一句：**让一个 SKU 有唯一一套数据口径，并且能直接支撑决策。**

---

## 三、我具体做了什么（对应简历那句，分五块）

### 1. 多店铺数据打通 + SKU 级 BI

- **打通三个数据源**：
  - 店铺后台 → Ozon 官方接口（商品、销售、财务、订单履约、退货、广告）
  - 公司数据中台 → 采购/申购/头程数据、Ozon 产品与库存数据
  - 运营表格 → 运营维护的直发表、SKU 管理表
- **统一到 SKU 级**：把上面所有数据按 SKU 聚合，做成一张核心的日汇总表，前端看板直接查它，速度很快。
- **BI 展示**：一个看板按 SKU 看利润、销量、库存，还能按店铺、日期、SKU 筛选，包含汇总卡、趋势图、库存健康、退货、广告、订单、利润、异常等模块。

> **可以直接讲的效果**：打通中台之后，SKU 管理表 55 列里有 **43 列能自动或半自动获取，人工填写从约 40 列降到 17 列**，录入工作量大幅下降。

### 2. 成本利润模块

分两层算，这是我最想讲清楚的一点：

- **平台毛利**：把 Ozon 的各项费用都算进去——佣金、物流费、仓储费、广告费、推广费、退货退款、其他费用，用收入加这些费用（费用是负数）得出净利润和利润率。
- **真实净利**：再扣掉**产品成本**（采购成本 + 送仓费 + 头程费用），按销量和汇率折算，让老板看到的是"真正落袋的钱"，而不是只看平台流水。

还做了**单 SKU 单件核算**：外箱体积、密度、头程费用、平台打款、实际回款、利润、利润率，以及**反推"要做到 3%/5%/10% 利润率的售价应该是多少"**，直接用在新品定价上。

> **诚实口径（被问到要主动说）**：真实净利是**经营参考的近似值**（产品成本含税估算、汇率用固定值），跟公司财务对账要以财务为准。我在系统里也标注了这一点。

### 3. 销量预测补货模块

- **预测日销**：不只看一天，而是把近 3 天、7 天、14 天、30 天的销量按不同权重加权，算出"加权日均销量"——近期权重高一些，反应更灵敏，同时用长周期压住波动。
- **补货量公式**（业务话术）：
  > 补货量 = 预测日销 ×（安全天数 + 物流天数）− 现有库存 − 在途库存
- **在途库存也算进去**：跨境在途（货代已收货、还没上架）+ 国内在途（国内备货），避免重复补货。
- **输出预警**：算出"可售天数"，低于安全天数标**紧急断货**，低于"安全+物流天数"标**预警**，否则正常。

### 4. 物流直发模块

- 把运营原来在表格里维护的**直发跟进**搬上线：SKU 基础数据、发货跟进表（申购单号、供应商、物流商、头程单号、箱数、收货状态等）。
- 支持**附件管理**（标签、箱唛、入库清单）和 **Excel 导入导出**，导出结构和原来的表格目录保持一致，运营可以平滑过渡。
- 这个模块同时是**补货模块"在途量"的数据来源**，两个模块是打通的。

### 5. 钉钉机器人通知

- 接入钉钉机器人（webhook + 加签），**货件状态改变时自动推送**，目前主要是"直发收货上架"时通知相关人。
- 用后台异步发送，**失败不影响业务**，没配置就静默跳过。

### 6. 顺带做的：异常预警

把业务口径做成**可配置的规则**（不用改代码就能调），自动扫出问题 SKU：断货风险、滞销积压、退货异常、利润率过低、广告花费过高、高点击低转化。

---

## 四、服务了谁、价值是什么

| 部门 | 他原来的痛点 | 现在 |
|---|---|---|
| 运营 | 手工对账、盯后台、凭感觉调价 | 一套 SKU 级数据看利润/销量/库存，异常自动预警，货件状态自动通知 |
| 供应链 | 补货靠拍脑袋，容易断货或积压 | 按预测日销 + 在途算建议补货量，有断货预警 |

**一句话价值**：数据口径统一了，运营和供应链看同一套数字；利润算到"落袋"；补货和预警从人工判断变成系统提示。

---

## 五、可以拿出来说的数字

> ⚠️ 上场前请按你**当前实际**规模核对一遍，用最新的数。

- 支持**多店铺**（每店独立接口配置，定时任务逐店跑）
- 已打通 **3 个数据源**（店铺后台 / 公司数据中台 / 运营表格）
- SKU 管理 **55 列中 43 列自动或半自动**，人工填写约 **40 列 → 17 列**
- 覆盖商品 39 个 SKU、订单约 1900 条、财务流水约 5700 条、退货 360 余条、广告活动 100 余个，日汇总约 2900 行、覆盖约 115 天
- 定时任务**每天自动同步**（含价格/库存每日快照），异常规则 **6 类**

---

## 六、口述讲稿

### 60 秒版（自我介绍/电梯汇报）

> 我搭了一套 Ozon 多店铺的数据智能平台。之前我们店铺后台、公司数据中台、运营的表格三处数据是散的，同一个 SKU 的利润、库存、销量对不上。我把这三处打通，统一成 SKU 级的利润、销量、库存 BI 看板；然后做了三个计算模块——成本利润，把 Ozon 各项费用和产品成本都算进去，看到真正落袋的利润；销量预测补货，用加权日均销量加上在途库存算建议补货量和断货预警；还有物流直发，把运营的直发表搬上线。最后接入钉钉机器人，货件状态一变就自动通知。服务的是运营和供应链两个部门，核心价值就是**数据口径统一，决策有依据**。

### 3 分钟版（在 60 秒版基础上按这个顺序展开）

1. **痛点**（30 秒）：三处数据散、口径不一致、决策靠拍脑袋。
2. **打通 + BI**（40 秒）：三个数据源怎么并到一起、统一到 SKU 级、看板能看什么；补一句"中台打通后人工填写从 40 列降到 17 列"。
3. **三个计算模块**（70 秒）：成本利润分平台毛利和真实净利两层；补货讲加权日销和"日销×（安全+物流天数）− 库存 − 在途"；直发讲把表格搬上线并给补货提供在途数据。
4. **钉钉通知**（15 秒）：货件状态变自动推送，异步不影响业务。
5. **价值**（25 秒）：运营和供应链用同一套数据，利润算到落袋，补货和预警自动化。

---

## 七、可能被问到的问题 + 怎么答

**Q：销量预测用的是什么模型？**
> 用的是**加权移动平均**，把近 3/7/14/30 天销量按权重算出日销，再结合安全天数和物流天数算补货量。**不是机器学习**——我们 SKU 多、历史数据还在积累，用可解释、运营能自己调天数的补货公式更务实。数据够了可以再升级。

**Q：你的利润算得准吗？跟财务对得上吗？**
> 分两层：**平台毛利**直接来自 Ozon 财务流水，口径是准的；**真实净利**是再扣掉产品成本算的，属于**经营参考的近似值**，有偏差（产品成本含税估算、汇率用固定值），对账以财务为准。我在系统里就标注了这一点。

**Q：多店铺数据会不会串？**
> 不会。每张表都有店铺标识，所有查询和同步都带店铺过滤，每家店用自己独立的接口凭证。

**Q：同一个 SKU 多个口径，你怎么统一？**
> 用货号（offer_id）和 Ozon 的 SKU 编号做关联键，统一聚合成一张 SKU 日汇总表，所有看板和计算模块都从这张表取数，保证口径唯一。

**Q：补货为什么要把"在途"算进去？**
> 因为在途的货还没到仓，如果不算进去会重复补货、造成积压。跨境在途和国内在途我分别统计，来源就是物流直发模块的数据。

**Q：钉钉通知失败了会影响业务吗？**
> 不会。通知是后台异步发的，失败只记日志；没配置就直接跳过，主流程不受影响。

**Q：库存没有历史快照，趋势怎么看？**
> Ozon 库存接口只返回当前值，所以我**每天定时把价格和库存做一次快照存下来**，这样就有历史趋势了。

**Q：这系统上线了吗？谁在用？**
> 服务**运营和供应链**两个部门，日常在用。定时任务每天自动同步数据。

**Q：你个人负责了哪些？**
> （**提前想好这题**：说清你是牵头全栈、还是负责其中哪几块。是你的就说透，是协作的就划清边界并强调你负责部分的难点。）

---

## 八、讲的时候别踩的坑

1. **别说"AI 预测"**——预测部分是加权移动平均，说"预测算法/加权日均"就行。
2. **别说利润"完全准确"**——真实净利是经营参考，主动提"对账以财务为准"反而是加分。
3. **"数据中台"是真实存在的**——公司确实有中台（采购/头程、Ozon 产品库存），你接的是真实中台，可以放心讲。
4. **别堆技术名词**——面试官更想听"解决了什么问题、谁在用、带来什么变化"。
5. **数字要用你现在的真实值**——上面第五节的数字是文档快照，上场前核对。

---

## 九、如果被深挖，心里有底的三件事

1. **利润口径曾经有三套说法**，我自己做过一次诊断，找出"同一个商品利润打架"的根因并推动统一——这说明我会**审计自己系统的口径一致性**，不只是会写功能。
2. **费用归因有讲究**：费用不是按 Ozon 结算日记账，而是**按下单日归因**，这样收入和成本在同一天对得上。
3. **广告数据是异步的、很慢**（平台一次只允许一个报告在跑），所以我把广告明细单独放在最后调度，避免拖慢主要同步。

> 这三点属于"加分细节"，**不主动讲，被问到再展开**。
